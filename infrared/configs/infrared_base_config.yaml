---
# InfraRed Configuration file which will contain Plugin locations. !!!!DO NOT REMOVE!!!!
repos:
  # Infrared provisioner.
  provisioning: 'https://github.com/rhos-infra/ir-provision.git'

  # Infrared installer.
  installer: 'https://github.com/rhos-infra/ir-installer.git'

  # Infrared tester.
  tester: 'http://localhost/tester.git'

  # Infrared cleanup.
  cleanup: 'http://localhost/cleanup.git'

# Infrared plugin install path.
plugin_path: '/tmp/infrared/plugins'

# Infrared artifact path.
logging:
  log_dir: '/tmp/infrared/logs'
  log_level: 'INFO'
